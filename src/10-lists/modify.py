def change_start(value, lst):
    """Change the first element (index 0) of the list to the given value."""
    lst[0] = value
t_list = ['a', 'b', 'c', 'd', 'e', 'f']
change_start('gg', t_list)
print(t_list)
